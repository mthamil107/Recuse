# Live-server experiment harness — contains real host IP / username. Never publish.
experiments/openfga/
experiments/**/_lib.py

# Server credential files (defensive; these live outside the repo anyway)
*.env
*.pem
*.key

# Phase 2 experiment secrets + raw transcripts (API keys, real host, trial logs)
**/secrets.local.json
experiments/phase2/secrets.local.json
experiments/phase2/results/
experiments/phase2/raw/

# AgentGovBench secrets, raw per-trial transcripts, and derived result tables.
experiments/agentgovbench/secrets.local.json
experiments/agentgovbench/results/
experiments/agentgovbench/results_table.csv
experiments/agentgovbench/results_table.txt
experiments/agentgovbench/leaderboard/leaderboard.json

# Authority-hierarchy experiment (Task #5) — per-trial records + transcripts.
experiments/authority/results/
experiments/authority/secrets.local.json

# Compiled proxy binary — built from source via adapters/postgres/README.md.
adapters/postgres/bin/
# Compiled webhook binary — built from source via adapters/kubernetes/README.md.
adapters/kubernetes/bin/
# Generated TLS material from hack/gen-certs.sh (never commit private keys).
adapters/kubernetes/hack/certs/
*.exe

# LaTeX build artifacts + the portable tectonic engine (not source)
paper/tectonic.zip
paper/tectonic_bin/
paper/*.aux
paper/*.log
paper/*.out
paper/*.bbl
paper/*.blg
paper/*.toc
paper/arxiv-submission.zip

# Debian build artifacts (dpkg-buildpackage writes these into packaging/)
packaging/*.deb
packaging/*.buildinfo
packaging/*.changes
packaging/deb/debian/.debhelper/
packaging/deb/debian/recuse-ssh/
packaging/deb/debian/files
packaging/deb/debian/*.substvars
packaging/deb/debian/*.debhelper.log

# Python
__pycache__/
*.pyc
.venv/
venv/

# OS / editor
Thumbs.db
.DS_Store
.vscode/
.idea/

# paper-stop build artifacts
paper-stop/*.aux
paper-stop/*.log
paper-stop/*.out
paper-stop/*.bbl
paper-stop/*.blg
paper-stop/*.toc
paper-stop/arxiv-submission.zip

# paper-tmlr build artifacts + bundle
paper-tmlr/*.aux
paper-tmlr/*.log
paper-tmlr/*.out
paper-tmlr/*.bbl
paper-tmlr/*.blg
paper-tmlr/*.toc
paper-tmlr/tmlr-submission.zip

# TMLR anonymized submission bundle (build artifact)
paper-tmlr/tmlr-submission-anon.zip

# PyPI package build artifacts
packaging/pypi/dist/
packaging/pypi/build/
packaging/pypi/*.egg-info/
packaging/pypi/src/*.egg-info/
paper-tmlr/recuse-arxiv-v3.tar.gz
