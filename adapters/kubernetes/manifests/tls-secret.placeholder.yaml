# PLACEHOLDER ONLY — do NOT apply this file as-is.
#
# The real Secret is created by hack/gen-certs.sh, which generates a self-signed
# CA + server cert (SANs for recuse-webhook.recuse-system.svc) and runs the
# equivalent of:
#
#   kubectl -n recuse-system create secret tls recuse-webhook-tls \
#     --cert=server.crt --key=server.key
#
# Or use cert-manager (see README) to manage this Secret automatically.
#
# This stub documents the expected shape so readers know what the Deployment
# mounts. It is intentionally EXCLUDED from kustomization.yaml.
apiVersion: v1
kind: Secret
metadata:
  name: recuse-webhook-tls
  namespace: recuse-system
  labels:
    app.kubernetes.io/name: recuse-webhook
    app.kubernetes.io/part-of: recuse
type: kubernetes.io/tls
data:
  tls.crt: "" # base64 PEM server certificate (filled by gen-certs.sh)
  tls.key: "" # base64 PEM server private key (filled by gen-certs.sh)
